<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itspring.cloud.mapper.PaymentMapper">
    <insert id="create" parameterType="PaymentPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO SPRING_CLOUD_PAYMENT(serial) values(#{serial});
    </insert>

    <resultMap id="BaseResrltMap" type="cn.itspring.cloud.entity.po.PaymentPO">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <id column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="getPaymentById" parameterType="Long" resultMap="BaseResrltMap">
        SELECT * FROM SPRING_CLOUD_PAYMENT where id=#{id};
    </select>

</mapper>